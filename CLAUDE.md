@AGENTS.md

# Default Workflow

Always use the /tdd skill (Red-Green-Refactor) when writing or modifying code. Every feature, bug fix, and refactor must go through the TDD cycle: write a failing test first, make it pass with minimal code, then refactor. Do not write production code without a failing test.
